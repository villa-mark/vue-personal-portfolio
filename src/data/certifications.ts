import type { Certification } from './types'

/**
 * Empty by default — intentionally demonstrates the "Certifications will be
 * added here" empty state. Add entries here once real certifications exist.
 */
export const certifications: Certification[] = []
